name: Api Practice Yourself CD - Production

on:
  push:
    branches: [ "main" ]

jobs:
  deploy_production:
    runs-on: self-hosted

    steps:
    - name: Change direction
      shell: bash
      run:
        cd ${{ secrets.SERVER_DIR }} && git checkout main && git pull && composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist && php artisan migrate --force
    - name: Give Permission
      shell: bash
      run:
        chmod -R 775 ${{ secrets.SERVER_DIR }}storage && chmod -R 775 ${{ secrets.SERVER_DIR }}bootstrap/cache
    # - name: Install update
    #   shell: bash
    #   run:
